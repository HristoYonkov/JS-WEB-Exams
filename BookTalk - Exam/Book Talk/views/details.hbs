<section class="details-page">
    <h1>Details</h1>
    <article class="details-card">

        <article class="details-card-text">
            <h2>Title:<br>{{book.title}}</h2>
            <h3>Author: {{book.author}}</h3>
            <h3>Stars: {{book.stars}}</h3>
            <h3>Genre: {{book.genre}}</h3>
            <h3>Review: {{book.rivew}}
            </h3>

            <!-- If there is no registered user, do not display buttons-->
            {{#if user}}
            <div class="buttons">
                <!-- Only for registered user and author of the review -->
                {{#if user._id == book._id}}
                <a href="/book/edit/{{_id}}" class="btn-edit">Edit</a>
                <a href="/book/delete/{{_id}}" class="btn-delete">Delete</a>
                {{/if}}
                <!-- logged in user who has not yet wished book-->
                <a href="#" class="btn-wish">Wish to read</a>
                <!-- logged in user who has already wished book-->
                <p class="wish-pub">You already added the book to your wish list</p>
            </div>
            {{/if}}
        </article>

        <article class="details-card-image">
            <img src="./image/Game-of-thrones.jpg">
        </article>

    </article>
</section>